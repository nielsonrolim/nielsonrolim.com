<%= turbo_stream.replace dom_id(@entry, :clip) do %>
  <%= render "reader/entries/clip_state", entry: @entry %>
<% end %>

<%= turbo_stream.append "toasts",
      partial: "layouts/toast",
      locals: { type: :notice, message: t("reader.entries.clip_success", title: @entry.title) } %>
